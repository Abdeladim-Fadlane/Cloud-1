name: Test Docker Push
on:
  push:
    branches: [ main ]
jobs:
  test-push:
    runs-on: ubuntu-latest
    steps:
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: afadlanee
          password: dckr_pat_rPRtCyZIOK_qKjEko3FmuYYbRQk
          
      - name: Test Push Simple Image
        run: |
          echo "FROM alpine:latest" | docker build -t afadlanee/test:latest -
          docker push afadlanee/test:latest
          
      - name: Verify Push Success
        run: echo "✅ Successfully pushed test   image to Docker Hub!"
