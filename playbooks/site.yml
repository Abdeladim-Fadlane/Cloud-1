- name: Playbook to set up the cloud-1 environment
  hosts: cloud-1
  become: true

  roles:
    - system-setup
    - certs
    - nginx
    - wordpress
    - mariadb
    - docker-compose
    # - cleanup
